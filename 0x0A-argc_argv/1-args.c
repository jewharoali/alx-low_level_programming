#include <stdio.h>

/**
 * main - main function
 * @argc:argument count
 * @argv:character array
 * Return:0 if successful
 */
int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", argc);
	return (0);
}
